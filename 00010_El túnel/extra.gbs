function hayParedAl(direccion) {
    ret := puedeMover(direccion)
    
    if (ret) {
      Mover(direccion)
    }
    
    return (ret && hayPared())
}

function hayPared() {
  return (hayBolitas(Azul))
}

procedure PicarPared() {
  Sacar(Azul)
}