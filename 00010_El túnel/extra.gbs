function hayParedAl(direccion) {
    puede := puedeMover(direccion)
    
    if (puede) {
      Mover(direccion)
    }
    
    return (puede && hayPared())
}

function hayPared() {
  return (hayBolitas(Azul))
}

procedure PicarPared() {
  Mover(Este)
  Sacar(Azul)
  Mover(Oeste)
}